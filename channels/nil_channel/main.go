package main

func main() {
	// var ch chan int // This is a nil channel
	//  ch <- 10 // This will panic because the channel is nil
	// <- ch // This will block because the channel is nil

	// value := <-ch // This will block because the channel is nil
	// fmt.Println(value)
}
